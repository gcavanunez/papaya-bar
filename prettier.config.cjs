/** @type {import("prettier").Config} */
const config = {
	plugins: ['prettier-plugin-tailwindcss'],
	useTabs: true,
	singleQuote: true,
	printWidth: 100,
	semi: false,
	tabWidth: 4,
}

module.exports = config
